set:
  log_errors:                  on
  arg_separator.output:        |
    &amp;

check:
  # zend.ze1_compatibility_mode: off

warn:
  register_globals:            off
  session.auto_start:          off